<!-- function is_prime($number) {
    if ($number <= 1) {
        return false;
    }
    for ($i = 2; $i < sqrt($number); $i++) { // Changed to < sqrt($number)
        if ($number % $i == 0) {
            return false;
        }
    }
    return true;
}

// Example usage:
$number = 17;
if (is_prime($number)) {
    echo "$number is a prime number.";
} else {
    echo "$number is not a prime number.";
} -->
